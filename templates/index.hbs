{{> header}}

<div class="container-fluid m-2 p-5 bg-primary text-center">
    <h1>Les Marvels</h1>
</div>
<div class="container m-2 p-5 bg-primary  text-center">
    <h2>Mes comics</h2>
    <div class="row">
        <ul>
            {{#each data}}
                <li>
                    <img src= {{ this.imageUrl }}  alt="Image du héros">
                    <h2>{{ this.name }}</h2>
                </li>
            {{/each}}
        </ul>
    </div>
</div>

{{> footer}}